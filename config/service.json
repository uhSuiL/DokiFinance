{
  "logger": {
    "logdir": "./log/",
    "handler": [
      {"type": "console", "level": "INFO", "format": "%(asctime)s[%(levelname)s]: %(message)s", "output": null},
      {"type": "sqlite", "level": "INFO", "format": "%(asctime)s[%(levelname)s]: %(message)s", "output": "logdb.db"}
    ]
  }
}